<h1>Listing Forms</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Active until</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for form <- @forms do %>
    <tr>
      <td><%= form.name %></td>
      <td><%= form.active_until %></td>

      <td>
        <span><%= link "Show", to: Routes.form_path(@conn, :show, form) %></span>
        <span><%= link "Edit", to: Routes.form_path(@conn, :edit, form) %></span>
        <span><%= link "Delete", to: Routes.form_path(@conn, :delete, form), method: :delete, data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Form", to: Routes.form_path(@conn, :new) %></span>
